# Máy LOCAL — chỉ chạy tiền xử lý dữ liệu (data_processing/), KHÔNG train.
# Train chạy trên Kaggle: xem requirements-kaggle.txt
#
# Kiểm tra sau khi cài:  python scripts/check_env.py --target local
# Cần Python >= 3.10 (3_uniprot_mapping.py và 4_build_ppi_graph.py dùng cú
# pháp `str | None` không có `from __future__ import annotations`).

# --- Đọc PDB / FASTA / contact map ---
biopython          # 2_extract_struct_map, get_sequence, 5_build_seq_feature
scipy              # ma trận kề contact map
numpy
pandas             # valid_protein_ids.csv
tqdm

# --- Gọi UniProt REST API (ENSP -> UniProtKB) ---
requests           # 3_uniprot_mapping

# --- Sequence embedding (bước nặng nhất, nên có GPU) ---
# torch CPU đủ cho tiền xử lý. Muốn ESM-2 nhanh thì cài bản CUDA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch
fair-esm           # 5_build_seq_feature (ESM-2)

# --- Đồ thị ---
# dgl KHÔNG có trên PyPI cho mọi nền tảng — cài từ wheel index của DGL:
#   pip install dgl -f https://data.dgl.ai/wheels/repo.html
# Bản cài phải khớp torch đang dùng; check_env.py mục 3 kiểm tra điều này.
dgl>=1.1.2         # 4_build_ppi_graph, 3_build_graph_dataset
