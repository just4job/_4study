# File này giữ lại cho tương thích — nó trỏ sang bộ requirements của MÁY LOCAL
# (tiền xử lý dữ liệu). Train chạy trên Kaggle, dùng bộ khác.
#
#   Máy local (tiền xử lý):  pip install -r requirements-local.txt
#   Kaggle   (train/eval) :  pip install -r requirements-kaggle.txt
#   Kiểm tra môi trường   :  python scripts/check_env.py
#
# Lưu ý: environment.yml trong repo là env CŨ của Struct2GO gốc
# (Python 3.7 + tensorflow + allennlp) — KHÔNG dùng được với code hiện tại,
# vốn cần Python >= 3.10. Đừng `conda env create -f environment.yml`.

-r requirements-local.txt
